#!/bin/bash

# Simple wrapper for the Python resume builder
# Usage: ./build-resume [variant]

# Use the same Python that's currently active in the environment
exec /opt/miniconda3/bin/python3 build-clean.py "$@"